# JARVIS - Whitelist approach: Ignore everything, then keep only what we need

# Ignore everything by default
*

# But keep these essential files and directories
!.gitignore
!README.md
!requirements.txt
!jarvis_agent.py
!launch_jarvis.bat
!launch_jarvis.ps1

# Keep all Python module files
!*.py

# Keep .git directory (needed for version control)
!.git/
!.git/**

# Keep .cursor directory (for Cursor IDE configuration)
!.cursor/
!.cursor/**

# Keep any other configuration files you might add
!.github/
!.github/**

# Note: chroma_db/, documents/, __pycache__/, and all temp files are automatically ignored
