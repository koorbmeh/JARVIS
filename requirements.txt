# JARVIS Unified Agent - Required Dependencies
# Install with: pip install -r requirements.txt

# Core LangChain
langchain==0.3.27
langchain-community==0.3.29
langchain-core==0.3.80

# Vector Database (RAG)
chromadb==1.0.20

# Web Search
beautifulsoup4
requests

# Web Interface
fastapi
uvicorn[standard]
websockets
python-multipart

# Computer Control
PyAutoGUI==0.9.54

# Document Processing
pypdf
python-docx
python-pptx

# Image Processing (Vision)
Pillow
pytesseract

# Windows-specific (optional, for better window focus in multi-display setups)
pywin32; sys_platform == "win32"

# Voice (Speech-to-Text and Text-to-Speech)
# faster-whisper is 4-5x faster than openai-whisper (recommended)
faster-whisper
# openai-whisper  # Fallback if faster-whisper not available
pyaudio
soundfile
# TTS options (install at least one)
pyttsx3  # Simple cross-platform TTS (recommended for quick start)
# piper-tts  # Higher quality TTS (optional, may require manual setup)

# PyTorch with CUDA support (for GPU acceleration of Whisper)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or for CPU-only: pip install torch torchvision torchaudio
torch>=2.0.0
